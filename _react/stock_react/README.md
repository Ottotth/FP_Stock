FP Stock Frontend
=================

This is a minimal Vite + React frontend for the FP_Stock project.

Quick start

```bash
cd _react/stock_react
npm install
npm run dev
```

The app expects the backend API at `/api/heatmap`. Adjust the URL in `src/App.jsx` if needed.

Build

```bash
npm run build
npm run preview
```
